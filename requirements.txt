# Used by: every model file, all training loops
torch>=2.0.0

# torchvision — image transforms, dataset utilities
# Used by: data/augmentations.py, byol.py and yolov12.py
torchvision>=0.15.0

# Ultralytics — provides YOLOv12 pretrained weights and training/inference API
# Used by: yolov12_proposal.py
ultralytics>=8.0.0

# Pillow — reading image dimensions in prepare_opixray.py
# Used by: data/prepare_opixray.py
Pillow>=9.0.0

# BERT text encoder (Stage 3)
# Used by: vision_language_detector.py
transformers==5.7.0

 
# torchmetrics — MeanAveragePrecision (mAP, AP50, AP75) for Stage 3 evaluation
# Used by: main.py run_stage3() evaluation block
torchmetrics>=1.0.0

# pycocotools — standard COCO mAP evaluator for fair comparison with paper
# Used by: evaluation scripts in main.py
# Note: on Windows install with: pip install pycocotools-windows
pycocotools>=2.0.0